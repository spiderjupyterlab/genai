pip install transformers torch
from transformers import pipeline

sentiment_analyzer = pipeline("sentiment-analysis")
#Default Hugging Face sentiment model (DistilBERT-based)

while True:
    user_input = input("\nEnter a sentence (or type 'exit' to quit): ")

    if user_input.lower() == "exit":
        print("Goodbye!")
        break

    if not user_input.strip():
        print("Please enter a non-empty sentence.")
        continue

    result = sentiment_analyzer(user_input)[0]

    print(f"\nLabel: {result['label']}")
    print(f"Confidence: {result['score']:.4f}")print("Sentiment Analysis Results:\n")
for sentence, result in zip(input_sentences, results):
    print(f"Input Sentence: {sentence}")
    print(f"Predicted Sentiment: {result['label']}, Confidence Score: {result['score']:.2f}\n")
